
d = 1;
Ib = 0.003;


            Qf = 72*(d/4)^0.635*pi*(d/2)^2*Ib^0.5;% Hennings
            h_init=0:d/1000:d;
            for t = 1:1001
                Q_initialize(t)=(0.46 - 0.5 *cos(pi*(h_init(t)/d))+0.04*cos(2*pi*(h_init(t)/d)))*Qf;
               div_fitty(t)= (0.5 *sin(pi*(h_init(t)/d))+0.04*cos(2*pi*(h_init(t)/d)))*Qf;
                
            end
           fitty = fit(Q_initialize',h_init','poly9');