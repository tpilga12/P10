function Xs = NewtonRoot(Fun,FunDer,Xest,Err,imax)
% NewtonRoot finds the toof of fun = 0 near the point Xest using newton's
% method.
% Input varui



for i = 1:imax
    Xi = Xest - feval(Fun,Xest)/feval(FunDer,Xest);
    if abs((Xi-Xest)/Xest) < Err
        Xs = Xi;
        break
    end
    Xest = Xi;
end 
if i == imax
    fprintf('solution was not observed in %i iterations .\n',imax)
    Xs = ('no answer');
end